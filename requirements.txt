fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
numpy>=1.26
# Linux servers use the CPU-only build (much smaller download); Windows/macOS use the normal one
xgboost-cpu>=3.1,<4; sys_platform == "linux"
xgboost>=3.1,<4; sys_platform != "linux"
